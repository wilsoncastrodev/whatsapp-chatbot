import formCalculateDistance from '../forms/formCalculateDistance.component.js';

const calculateDistance = async (message, client) => {
    await formCalculateDistance(message, client);
}

export default calculateDistance;
